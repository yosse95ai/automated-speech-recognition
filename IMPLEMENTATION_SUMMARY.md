# CIDR検証実装 - 実装サマリー

## 実装完了

このドキュメントは、automated-speech-recognition CDKプロジェクトに実装されたCIDR検証機能の概要を説明します。

## 実装された機能

### 1. VPC CIDR検証
- **ルール**: プレフィックス長が /25 より大きいCIDRブロックを拒否
- **対象**: `apiVpcCidr` および `onpremiseCidr` 環境設定
- **拒否される例**: /26, /27, /28, /29, /30, /31, /32
- **許可される例**: /16, /20, /24, /25

### 2. サブネット CIDR検証
- **ルール**: プレフィックス長が /27 より大きいCIDRマスクを拒否
- **対象**: VPC構成内のすべてのサブネットCIDRマスク
- **拒否される例**: /28, /29, /30, /31, /32
- **許可される例**: /16, /20, /24, /27

### 3. エラーメッセージ
- すべてのエラーメッセージは日本語で提供
- 無効なCIDR値と要件を明確に説明
- 具体的な例を含む

## 作成/変更されたファイル

### 新規作成

1. **`packages/cdk/lib/utils/cidr-validation.ts`**
   - CIDR検証ユーティリティ関数
   - `validateVpcCidr()`: VPC CIDR検証
   - `validateSubnetCidr()`: サブネット CIDR検証
   - 再利用可能な検証ロジック

2. **`packages/cdk/lib/validators/environment-validator.ts`**
   - CDK `IValidation` インターフェースを実装
   - スタックレベルでの環境設定検証
   - CDK合成時の早期エラー検出

3. **`packages/cdk/test/cidr-validation.test.ts`**
   - 包括的な単体テスト
   - VPC CIDR検証のテストケース
   - サブネット CIDR検証のテストケース
   - エラーメッセージ内容の検証

4. **`CIDR_VALIDATION_IMPLEMENTATION.md`**
   - 詳細な実装アプローチの説明
   - 検証パターンの比較
   - トレードオフの議論
   - テスト方法の説明

### 変更されたファイル

1. **`packages/cdk/lib/constructor/api/vpc.ts`**
   - VPC CIDR検証の追加
   - プライベート/パブリックサブネットCIDR検証の追加
   - コンストラクタレベルでの検証実装

2. **`packages/cdk/lib/constructor/onprem/vpc.ts`**
   - VPC CIDR検証の追加
   - プライベートサブネットCIDR検証の追加
   - コンストラクタレベルでの検証実装

3. **`packages/cdk/lib/main-app-stack.ts`**
   - `EnvironmentValidator`のインポート
   - スタックレベルでの検証追加（`this.node.addValidation()`）
   - 環境設定の早期検証

## 実装アプローチ

### 多層的な検証戦略

1. **スタックレベル検証**（優先度: 高）
   - CDK合成時に実行
   - 環境設定全体の整合性チェック
   - デプロイ前のエラー検出

2. **コンストラクトレベル検証**（優先度: 高）
   - リソース作成時に実行
   - 防御的プログラミング
   - コンストラクトの再利用性を保証

### 検証の流れ

```
1. CDK App起動
   ↓
2. MainAppStack作成
   ↓
3. スタックレベル検証実行（EnvironmentValidator）
   - apiVpcCidr検証
   - onpremiseCidr検証
   ↓
4. VPCコンストラクト作成
   ↓
5. コンストラクトレベル検証実行
   - VPC CIDR検証
   - サブネットCIDRマスク検証
   ↓
6. エラーがある場合: 処理を中止してエラーメッセージを表示
   エラーがない場合: リソース作成を続行
```

## エラーメッセージの例

### VPC CIDR検証エラー（/26の場合）

```
Api VPCのCIDRブロックのプレフィックス長が無効です: 10.0.0.0/26。
プレフィックス長は /25 以下である必要があります（現在: /26）。
/26、/27、/28 などのプレフィックスは使用できません。
```

### サブネット CIDR検証エラー（/28の場合）

```
Api プライベートサブネットのサブネットCIDRマスクが無効です: /28。
サブネットのプレフィックス長は /27 以下である必要があります。
/28、/29、/30 などのプレフィックスは使用できません。
```

## テスト方法

### 1. 検証のテスト（無効なCIDRを使用）

`bin/app.ts`を編集して無効なCIDRを設定:

```typescript
export const props: EnvironmentProps = {
  // ... 他の設定 ...
  apiVpcCidr: "10.0.0.0/28",  // これは失敗するはず（/28 > /25）
  onpremiseCidr: "10.128.0.0/16",
};
```

CDK合成を実行:

```bash
cd packages/cdk
npm run build
npx cdk synth
```

期待される結果: エラーメッセージが表示され、合成が失敗

### 2. 単体テストの実行

```bash
cd packages/cdk
npm test cidr-validation.test.ts
```

### 3. 完全なスタックのテスト

有効なCIDRで正常にデプロイできることを確認:

```bash
cd packages/cdk
npx cdk synth  # 合成が成功することを確認
npx cdk diff   # 変更内容を確認
```

## 検証ルールの根拠

### VPC CIDR: /25 以下の制限

- **/25以下を許可する理由**:
  - 適切な数のサブネットを作成するために十分なIPアドレス空間を確保
  - マルチAZ構成に対応
  - 将来の拡張性を考慮

- **/26以上を拒否する理由**:
  - /26 = 64個のIPアドレス（利用可能: 約59個）
  - /27 = 32個のIPアドレス（利用可能: 約27個）
  - /28 = 16個のIPアドレス（利用可能: 約11個）
  - VPC内に複数のサブネットを作成するには不十分

### サブネット CIDR: /27 以下の制限

- **/27以下を許可する理由**:
  - /27 = 32個のIPアドレス（利用可能: 約27個）
  - 小規模なサブネットに対応
  - AWSリソース（ENI、Lambda、ECS等）に十分なIPアドレスを提供

- **/28以上を拒否する理由**:
  - /28 = 16個のIPアドレス（利用可能: 約11個）
  - /29 = 8個のIPアドレス（利用可能: 約3個）
  - /30 = 4個のIPアドレス（利用可能: 約1個）
  - AWSが予約するIPアドレスを考慮すると実用的でない

## AWS CDKバリデーションパターンの参照

この実装は以下のAWS CDK推奨パターンに基づいています:

1. **IValidationインターフェースの使用**
   - CDK標準の検証メカニズム
   - `node.addValidation()`による統合

2. **コンストラクタでの検証**
   - 即座のエラー検出
   - 防御的プログラミング

3. **分離された検証ロジック**
   - ユーティリティ関数による再利用
   - テストが容易

## 今後の拡張の可能性

1. **追加のCIDR検証**
   - RFC 1918プライベートアドレス空間の確認
   - CIDR範囲の重複チェック
   - IPアドレス形式の詳細検証

2. **カスタマイズ可能な制限**
   - 環境変数による制限値の設定
   - プロジェクト固有のCIDR要件への対応

3. **より詳細なエラー報告**
   - 推奨されるCIDR値の提案
   - 利用可能なIPアドレス数の計算と表示

## まとめ

この実装により、automated-speech-recognition CDKプロジェクトは以下を実現しています:

✅ VPCおよびサブネット構成の堅牢なCIDR検証
✅ 日本語での明確なエラーメッセージ
✅ 複数レベルでの防御的な検証
✅ CDKベストプラクティスに従った実装
✅ 包括的なテストカバレッジ
✅ 保守性の高いコード構造
✅ 再利用可能な検証ユーティリティ

詳細な実装アプローチについては、`CIDR_VALIDATION_IMPLEMENTATION.md`を参照してください。
